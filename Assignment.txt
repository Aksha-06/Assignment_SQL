1)
SELECT ENAME, JOB
FROM EMP
WHERE JOB LIKE 'CLERK'; 

2)
SELECT ENAME, JOB
FROM EMP
WHERE JOB IN ('CLERK','MANAGER'); 

3)
SELECT CAST(GETDATE() AS DATE);

4)
SELECT ENAME, SAL
FROM EMP
WHERE SAL BETWEEN 500 AND 1000; 

5)
SELECT ENAME, HIREDATE
FROM EMP
WHERE MONTH(HIREDATE)='12';

6)
SELECT ENAME, SAL
FROM EMP
WHERE SAL NOT BETWEEN 1000 AND 2500;

7)
SELECT ENAME, SAL
FROM EMP
WHERE SAL IN(800, 950, 3000, 5000); 

8)
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '_L%';

9)
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '_%D'; 

10)
SELECT ENAME
FROM EMP
WHERE LENGTH(TRIM(ENAME))=5;

11)
SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'T%R'; 

12)
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%A%';

13)
SELECT ENAME, HIREDATE, DATEPART(DD,HIREDATE) AS 'DATE JOINED'
FROM EMP
WHERE DATEPART(DD,HIREDATE)<19;

14)
SELECT ENAME, E.EMPNO 'EMPNO'
FROM EMP E
WHERE ENAME = 'JAMES';

15)
SELECT ENAME, E.SAL 'SAL'
FROM EMP E
WHERE ENAME = 'KING';

16)
SELECT ENAME, JOB
FROM EMP
WHERE JOB LIKE 'PRESIDENT';

17) 
SELECT *
FROM EMP
WHERE JOB = 'MANAGER';

18)
SELECT *
FROM EMP
WHERE COMM IS NOT NULL;

19)
SELECT *
FROM EMP
WHERE HIREDATE<'1-APR-81';

20)21)
SELECT ENAME, SAL
FROM EMP
WHERE ENAME IN ('KING','BLAKE','FORD','SMITH');

22)
SELECT ENAME, SAL
FROM EMP
WHERE SAL<3500;

23)
SELECT *
FROM EMP
WHERE JOB = 'CLERK';

24)
SELECT ENAME, JOB, SAL*12 AS 'ANNUAL SALARY'
FROM EMP;

25)
SELECT ENAME, SAL*12*0.2
FROM EMP
WHERE ENAME='FORD';

26)
SELECT E.DEPTNO, D.DNAME, SUM(SAL) AS 'TOTAL SALARY'
FROM EMP E, DEPT D
WHERE E.DEPTNO=D.DEPTNO
GROUP BY E.DEPTNO;

27)
SELECT ENAME, MIN(SAL) AS 'LOWEST SALARY'
FROM EMP
WHERE DEPTNO=10
GROUP BY ENAME;

28)
SELECT ENAME, AVG(SAL) AS 'AVERAGE SALARY'
FROM EMP
WHERE JOB='MANAGER'
GROUP BY ENAME;

29)
SELECT ENAME, SUM(SAL) AS 'TOTAL SALARY'
FROM EMP
WHERE JOB='SALESMAN'
GROUP BY ENAME;

30)
SELECT DEPTNO, AVG(SAL) AS 'AVERAGE SALARY'
FROM EMP
GROUP BY DEPTNO
HAVING AVG(SAL)
               <
                 (SELECT AVG(SAL)
                 FROM EMP);

31)
SELECT DEPTNO, COUNT(*) AS EMPCOUNT
FROM EMP
GROUP BY DEPTNO;

32)
SELECT ENAME, SAL, DEPTNO
FROM EMP
WHERE SAL
         IN
            (SELECT MAX(SAL) AS 'HIGHEST SALARY'
             FROM EMP
             WHERE JOB LIKE'ANALYST');

33)
SELECT ENAME, MAX(COMM) AS 'HIGHEST COMMISSION'
FROM EMP
WHERE COMM IS NOT NULL
GROUP BY ENAME;

34)
SELECT COUNT(DEPTNO) AS 'DEPT COUNT'
FROM DEPT;

35)
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '__L%';
